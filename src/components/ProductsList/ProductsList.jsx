import React from 'react';


const ProductsList = (props) => {
  return (
    <div>
      {/* {props.products.map(product => <div key={product.sku}>{product.name}</div> 
      )}    */}
    </div>

  )
}

export default ProductsList; 